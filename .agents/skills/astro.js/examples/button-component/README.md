---
# Button Component Example
#
# Foundational Button built as a SolidJS component with a separate CSS Module file.
# Foundational components are NEVER .astro — they handle click events and typed callbacks.
#
# This example shows the SolidJS version. For a React project, replace:
#   - `class=` with `className=`
#   - Refer to the `react` skill for React-specific conventions.
#
# Files:
#   index.tsx           ← SolidJS component (or React .tsx in a React project)
#   styles.module.css   ← ALL styles, always a separate file
---
